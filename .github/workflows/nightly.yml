name: Ganatri Nightly Build

on:
  schedule:
    - cron: '7 */3 * * *'   # every 3 hours, at :07 (off the :00 congestion window so GitHub is less likely to drop the run)
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Run even if an open nightly/* PR exists (manual override)'
        required: false
        default: 'false'
        type: boolean

# Prevent overlapping runs
concurrency:
  group: ganatri-nightly
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  nightly:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
      SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Prune merged nightly branches
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
          git fetch origin --prune
          # Delete any nightly/* branch already merged into main; leaves unmerged work untouched.
          for b in $(git branch -r --merged origin/main | grep 'origin/nightly/' | sed 's#origin/##'); do
            echo "Deleting merged branch: $b"
            git push origin --delete "$b" || true
          done

      - name: Decide whether to run
        id: gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          FORCE_RUN="${{ github.event.inputs.force_run || 'false' }}"
          echo "force_run=${FORCE_RUN}" >> "$GITHUB_OUTPUT"

          # Skip if an unmerged nightly PR is already open — avoids re-doing the same task
          # while the previous run's work is sitting in review. Manual force_run bypasses this.
          OPEN_PR=$(gh pr list --base main --state open --json number,headRefName,title \
                      --jq '.[] | select(.headRefName | startswith("nightly/")) | "#\(.number) (\(.headRefName)) — \(.title)"' \
                      | head -1)
          if [ "$FORCE_RUN" != "true" ] && [ -n "$OPEN_PR" ]; then
            echo "Skipping — open nightly PR exists: ${OPEN_PR}. Merge or close it first (or re-run with force_run)."
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "priority=false" >> "$GITHUB_OUTPUT"
            echo "skip_reason=open_pr" >> "$GITHUB_OUTPUT"
            {
              echo "open_pr<<EOF"
              printf '%s\n' "$OPEN_PR"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Status is the first non-empty line after the "## Status" heading, minus any HTML comment.
          STATUS=$(awk '/^## Status/{getline; while ($0 ~ /^[[:space:]]*$/) getline; print; exit}' \
                     docs/nightly/STATE.md | sed 's/<!--.*-->//')
          echo "STATE.md status: ${STATUS}"
          if echo "$STATUS" | grep -q 'BLOCKED'; then
            echo "Skipping Claude run — project is BLOCKED, needs human input."
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "skip_reason=blocked" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi

          # Surface whether the Priority TODO queue has any unchecked items.
          # The body between the two markers contains unchecked "- [ ]" lines when non-empty.
          PRIORITY=$(awk '/<!-- PRIORITY_TODO:START -->/{f=1; next} /<!-- PRIORITY_TODO:END -->/{f=0} f' \
                       docs/DEVELOPMENT_PLAN.md | grep -c '^[[:space:]]*- \[ \]' || true)
          echo "Priority TODO unchecked items: ${PRIORITY}"
          if [ "${PRIORITY:-0}" -gt 0 ]; then
            echo "priority=true" >> "$GITHUB_OUTPUT"
            TOP_TASK=$(awk '/<!-- PRIORITY_TODO:START -->/{f=1; next} /<!-- PRIORITY_TODO:END -->/{f=0} f && /^[[:space:]]*- \[ \]/{print; exit}' \
                         docs/DEVELOPMENT_PLAN.md)
            echo "Top priority item: ${TOP_TASK}"
            echo "Priority queue is NON-EMPTY — Claude will work the top unchecked item (checked history below it is ignored)."
          else
            echo "priority=false" >> "$GITHUB_OUTPUT"
            echo "Priority queue empty — Claude will follow the normal phase flow."
          fi

          # Surface the likely unit of work for Slack start notifications.
          NEXT_TASK=$(awk '/<!-- PRIORITY_TODO:START -->/{f=1; next} /<!-- PRIORITY_TODO:END -->/{f=0} f && /^[[:space:]]*- \[ \]/' \
                        docs/DEVELOPMENT_PLAN.md | head -1 \
                        | sed -E 's/^[[:space:]]*- \[ \] //; s/\*\*([^*]+)\*\*.*/\1/')
          if [ -z "$NEXT_TASK" ]; then
            NEXT_TASK=$(awk '/^## Notes for Next Run$/{f=1; next} /^## /{if(f) exit} f && /^[0-9]+\./' \
                          docs/nightly/STATE.md | head -1 \
                          | sed -E 's/^[0-9]+\. //; s/\*\*([^*]+)\*\*.*/\1/')
          fi
          if [ -z "$NEXT_TASK" ]; then
            NEXT_TASK=$(awk '/^## Current Phase$/{getline; while ($0 ~ /^[[:space:]]*$/) getline; print; exit}' \
                          docs/nightly/STATE.md)
          fi
          {
            echo "next_task<<EOF"
            printf '%s\n' "$NEXT_TASK"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Notify Slack — gate skipped
        if: steps.gate.outputs.run != 'true' && steps.gate.outputs.skip_reason != ''
        continue-on-error: true
        env:
          OPEN_PR: ${{ steps.gate.outputs.open_pr }}
        run: |
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          if [ "${{ steps.gate.outputs.skip_reason }}" = "open_pr" ]; then
            MSG=":double_vertical_bar: *Nightly skipped* — an open \`nightly/*\` PR is waiting for review. Merge or close it before the next run picks up new work."$'\n'"*Open PR:* ${OPEN_PR}"$'\n'"Re-run manually with *force_run* checked to override."$'\n'"<${RUN_URL}|View run>"
          else
            MSG=":double_vertical_bar: *Nightly skipped* — project status is BLOCKED in \`docs/nightly/STATE.md\`. Needs human input before Claude can run."$'\n'"<${RUN_URL}|View run>"
          fi
          bash .github/scripts/slack-notify.sh "$SLACK_BOT_TOKEN" "$SLACK_CHANNEL_ID" "$MSG"

      - name: Create working branch
        id: branch
        if: steps.gate.outputs.run == 'true'
        run: |
          BRANCH="nightly/$(date +%Y-%m-%d-%H%M)"
          git checkout -b "$BRANCH"
          echo "name=$BRANCH" >> "$GITHUB_OUTPUT"

      - name: Notify Slack — run started
        if: steps.gate.outputs.run == 'true'
        continue-on-error: true
        env:
          NEXT_TASK: ${{ steps.gate.outputs.next_task }}
        run: |
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          if [ "${{ steps.gate.outputs.priority }}" = "true" ]; then
            MODE="priority TODO"
          else
            MODE="normal phase flow"
          fi
          TASK_LINE=""
          if [ -n "$NEXT_TASK" ]; then
            TASK_LINE=$'\n*Task:* '"$NEXT_TASK"
          fi
          TEXT=":hammer_and_wrench: *Ganatri nightly started* — branch \`${{ steps.branch.outputs.name }}\` (${MODE}).${TASK_LINE}"$'\n'"<${RUN_URL}|View run>"
          bash .github/scripts/slack-notify.sh "$SLACK_BOT_TOKEN" "$SLACK_CHANNEL_ID" "$TEXT"

      - name: Run Claude on next phase
        if: steps.gate.outputs.run == 'true'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            You are working autonomously overnight on the Ganatri project.
            No human is available — make safe, conservative decisions.

            STEP 1 — Read state and context:
            - Read docs/DEVELOPMENT_PLAN.md FIRST, and pay special attention to the
              "🔝 Priority TODO (read FIRST every run)" section near the top (the queue lives
              between the <!-- PRIORITY_TODO:START --> and <!-- PRIORITY_TODO:END --> markers).
            - Read docs/nightly/STATE.md (current phase, status, blockers, sequencing notes).
            - Read CLAUDE.md for project conventions and agent routing guidance.
            - Read docs/GAME_RULES.md before touching any game logic.

            STEP 2 — Pick exactly ONE unit of work:
            - If STATE.md says BLOCKED, stop immediately — do not attempt to work around the blocker.
            - PRIORITY QUEUE FIRST: look at the "🔝 Priority TODO" section in docs/DEVELOPMENT_PLAN.md.
                - If it has one or more unchecked "- [ ]" items, the TOP unchecked item is your unit
                  of work for this run. It OVERRIDES the current phase. Do it, then mark it "- [x]"
                  with today's date. Ignore the normal phase flow this run.
                - If the queue is empty (only the "_(none)_" placeholder between the markers), fall
                  through to the normal phase flow below.
            - NORMAL FLOW (only when the priority queue is empty): implement the next NOT_STARTED or
              IN_PROGRESS item from the current phase in STATE.md.
            - Do NOT start a second phase. One coherent unit of work per run, finished cleanly.
            - If the current phase has pull-forward sub-items (like the Phase 7 urgent fixes), tackle
              them one at a time in the listed order, updating STATE.md after each one.

            STEP 3 — Implement via agents (follow CLAUDE.md exactly):
            You are the coordinator. Do NOT write code directly — delegate to the correct agent:
            - packages/engine work → spawn Agent with subagent_type="rules-engine-dev"
            - packages/server work → spawn Agent with subagent_type="backend-dev"
            - packages/web work → spawn Agent with subagent_type="frontend-dev"
            - After implementation is done → spawn Agent with subagent_type="code-reviewer" to verify
            Parallelize independent tasks across agents where possible.
            After all agents complete, run `npm test` yourself to confirm everything passes.
            If tests still fail after 2 fix attempts via agents, STOP and record BLOCKED.

            STEP 4 — Update docs/nightly/STATE.md:
            - If the work unit is done and tests pass:
                - If more pull-forward items remain, update "Notes for Next Run" to reflect the next item.
                - If the entire current phase is done, set status COMPLETE, move it to "Completed Phases",
                  and set "Current Phase" to the next one with status NOT_STARTED.
            - If blocked: set status BLOCKED and write a clear, specific entry under
              "Blockers / Needs Human Input" — include file paths, error messages, what you tried.
            - Always fill in "Last Run" with today's date, outcome, and a one-line summary.
            - Also update docs/DEVELOPMENT_PLAN.md: mark completed tasks ✅, in-progress 🟡.
            - If this run completed a PRIORITY TODO item, mark that "- [ ]" line as "- [x]" with
              today's date in the "🔝 Priority TODO" section of docs/DEVELOPMENT_PLAN.md (leave it
              checked for history; do not delete it). If the queue is now all-checked, that is fine —
              the next run will fall back to the normal phase flow.

            STEP 5 — Commit:
            - Stage all changed files (code + STATE.md + DEVELOPMENT_PLAN.md).
            - Commit with a message like: "Nightly: <Phase name> — <task title> (complete|partial|blocked)"

            Hard rules:
            - Branch only — never touch main directly.
            - Prefer correctness over completeness. A clean partial result with an honest BLOCKED note
              is better than broken code committed to the branch.
            - Never skip tests. Never use --no-verify.
            - If you are unsure about a game rule, mark BLOCKED and ask — do not guess.
          claude_args: >-
            --model claude-sonnet-4-6
            --max-turns 60
            --allowedTools Edit,Read,Write,Bash,Agent

      - name: Generate development report
        if: steps.gate.outputs.run == 'true'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            Generate an updated Ganatri development-progress report for this run.

            IMPORTANT — environment: you are on a Linux CI runner (Ubuntu), NOT macOS.
            When the report-generator agent renders the PDF it must use the Linux Chrome
            binary, not the "/Applications/Google Chrome.app" path. Render with:
              google-chrome --headless=new --disable-gpu --no-pdf-header-footer \
                --virtual-time-budget=10000 \
                --print-to-pdf="<abs>/reports/<slug>-<ts>.pdf" \
                "file://<abs>/reports/<slug>-<ts>.html"
            (If `google-chrome` is missing, fall back to `which chromium-browser` / `chromium`.)

            Spawn Agent with subagent_type="report-generator". Tell it the topic is the
            Ganatri project's development progress, built from the current repo state:
            - docs/DEVELOPMENT_PLAN.md — phase-by-phase status and test counts.
            - docs/nightly/STATE.md — current phase, last-run outcome, and any blockers.
            - This run's work: inspect recent branch commits with
              `git log origin/main..HEAD --stat` to see what changed (may be empty on a
              no-op run — say so honestly in the report).

            The report title is "Ganatri — Development Progress" with today's date as the
            subtitle. It should summarize: the current phase, what was completed this run,
            overall progress across all phases, the current test status, and any blockers
            or next steps. Save under reports/ as the agent normally does.

            Do NOT commit anything and do NOT modify docs/DEVELOPMENT_PLAN.md — the report
            is published as a build artifact, not checked into git.
          claude_args: >-
            --model claude-sonnet-4-6
            --max-turns 30
            --allowedTools Edit,Read,Write,Bash,Agent

      - name: Extract run summary for Slack
        if: steps.gate.outputs.run == 'true'
        id: summary
        run: |
          LAST_DATE=$(awk '/^## Last Run$/{f=1; next} /^## /{if(f) exit} f && /^- Date:/{sub(/^- Date: /,""); print; exit}' \
                        docs/nightly/STATE.md)
          OUTCOME=$(awk '/^## Last Run$/{f=1; next} /^## /{if(f) exit} f && /^- Outcome:/{sub(/^- Outcome: /,""); print; exit}' \
                      docs/nightly/STATE.md)
          if [ "$LAST_DATE" != "$(date +%Y-%m-%d)" ]; then
            OUTCOME=""
          fi
          COMMIT=$(git log origin/main..HEAD --format=%s -1 2>/dev/null || true)
          PHASE=$(awk '/^## Current Phase$/{getline; while ($0 ~ /^[[:space:]]*$/) getline; print; exit}' \
                    docs/nightly/STATE.md)
          SUMMARY="${OUTCOME:-$COMMIT}"
          if [ -z "$SUMMARY" ]; then
            SUMMARY=""
          fi
          # Keep Slack messages readable if Claude wrote a long outcome line.
          if [ "${#SUMMARY}" -gt 500 ]; then
            SUMMARY="${SUMMARY:0:497}..."
          fi
          {
            echo "phase<<EOF"
            printf '%s\n' "$PHASE"
            echo "EOF"
            echo "commit<<EOF"
            printf '%s\n' "$COMMIT"
            echo "EOF"
            echo "summary<<EOF"
            printf '%s\n' "$SUMMARY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set report artifact name
        if: steps.gate.outputs.run == 'true'
        id: report-artifact
        run: echo "name=ganatri-development-report-$(date +%Y-%m-%d-%H%M%S)" >> "$GITHUB_OUTPUT"

      - name: Upload report artifact
        if: steps.gate.outputs.run == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.report-artifact.outputs.name }}
          path: reports/
          if-no-files-found: warn
          retention-days: 30

      - name: Send report zip to Slack
        if: steps.gate.outputs.run == 'true'
        continue-on-error: true
        env:
          RUN_SUMMARY: ${{ steps.summary.outputs.summary }}
          RUN_PHASE: ${{ steps.summary.outputs.phase }}
          RUN_COMMIT: ${{ steps.summary.outputs.commit }}
        run: |
          if [ -z "$(ls -A reports 2>/dev/null)" ]; then
            echo "No report files produced this run — nothing to send."
            exit 0
          fi
          ZIP="ganatri-development-report-$(date +%Y-%m-%d-%H%M).zip"
          (cd reports && zip -r "../${ZIP}" . >/dev/null)
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          COMMENT=":bar_chart: *Development report* — $(date +%Y-%m-%d\ %H:%M)"
          if [ -n "$RUN_PHASE" ]; then
            COMMENT="${COMMENT}"$'\n'"*Phase:* ${RUN_PHASE}"
          fi
          if [ -n "$RUN_SUMMARY" ]; then
            COMMENT="${COMMENT}"$'\n'"*This run:* ${RUN_SUMMARY}"
          elif [ -n "$RUN_COMMIT" ]; then
            COMMENT="${COMMENT}"$'\n'"*Commit:* ${RUN_COMMIT}"
          fi
          COMMENT="${COMMENT}"$'\n'"<${RUN_URL}|View run>"
          bash .github/scripts/slack-upload-file.sh \
            "$SLACK_BOT_TOKEN" "$SLACK_CHANNEL_ID" "$ZIP" \
            "Ganatri development report" \
            "$COMMENT"

      - name: Push branch
        id: push
        if: steps.gate.outputs.run == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "$(git rev-list --count origin/main..HEAD)" -eq 0 ]; then
            echo "No commits this run — skipping push (no empty branch left behind)."
            echo "pushed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
          git push origin "${{ steps.branch.outputs.name }}"
          echo "pushed=true" >> "$GITHUB_OUTPUT"

      - name: Open Pull Request
        id: pr
        if: steps.push.outputs.pushed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git rev-parse --verify origin/main >/dev/null 2>&1 && \
             [ "$(git rev-list --count origin/main..HEAD)" -gt 0 ]; then
            PR_URL=$(gh pr create \
              --base main \
              --head "${{ steps.branch.outputs.name }}" \
              --title "Nightly build — $(date +%Y-%m-%d\ %H:%M)" \
              --body "$(cat <<'EOF'
          ## Autonomous nightly run

          See \`docs/nightly/STATE.md\` for what was done, what's next, and any blockers.

          **Review before merging** — check diff for correctness against game rules in \`docs/GAME_RULES.md\`.

          🤖 Generated with [Claude Code](https://claude.ai/claude-code)
          EOF
          )" 2>/dev/null) || PR_URL=$(gh pr view "${{ steps.branch.outputs.name }}" --json url -q .url 2>/dev/null || true)
            echo "url=${PR_URL}" >> "$GITHUB_OUTPUT"
            echo "Pull request: ${PR_URL:-<none>}"
          else
            echo "No changes this run — no PR created."
          fi

      - name: Notify Slack — run finished
        if: steps.gate.outputs.run == 'true'
        continue-on-error: true
        env:
          RUN_SUMMARY: ${{ steps.summary.outputs.summary }}
          RUN_PHASE: ${{ steps.summary.outputs.phase }}
          RUN_COMMIT: ${{ steps.summary.outputs.commit }}
        run: |
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          if [ "${{ steps.push.outputs.pushed }}" = "true" ]; then
            ICON=":white_check_mark:"
            HEADLINE="*Ganatri nightly complete* — PR opened"
            FOOTER="${{ steps.pr.outputs.url }}"
          else
            ICON=":information_source:"
            HEADLINE="*Ganatri nightly finished* — no code changes (nothing to PR)"
            if [ "${{ steps.gate.outputs.priority }}" = "true" ]; then
              FOOTER="Expected a priority TODO to be implemented — check the *Run Claude* step logs (Claude may have exited without committing)."
            else
              FOOTER=""
            fi
          fi
          MSG="${ICON} ${HEADLINE}"
          if [ -n "$RUN_PHASE" ]; then
            MSG="${MSG}"$'\n'"*Phase:* ${RUN_PHASE}"
          fi
          if [ -n "$RUN_SUMMARY" ]; then
            MSG="${MSG}"$'\n'"*Outcome:* ${RUN_SUMMARY}"
          elif [ -n "$RUN_COMMIT" ]; then
            MSG="${MSG}"$'\n'"*Commit:* ${RUN_COMMIT}"
          fi
          if [ -n "$FOOTER" ]; then
            MSG="${MSG}"$'\n'"${FOOTER}"
          fi
          MSG="${MSG}"$'\n'"<${RUN_URL}|View run>"
          bash .github/scripts/slack-notify.sh "$SLACK_BOT_TOKEN" "$SLACK_CHANNEL_ID" "$MSG"

      - name: Notify Slack — run failed
        if: failure()
        continue-on-error: true
        run: |
          bash .github/scripts/slack-notify.sh "$SLACK_BOT_TOKEN" "$SLACK_CHANNEL_ID" \
            ":x: *Ganatri nightly failed* — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run logs>"
